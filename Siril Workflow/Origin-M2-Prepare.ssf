requires 1.2.0

# convert lights
cd lights
convert light -out=../process
cd ../process

# calibrate the lights with  the bias/dark/flat master
calibrate light -bias=../masters/bias_master -dark=../masters/dark_master -flat=../masters/flat_master -cfa -equalize_cfa -debayer

# Ausrichtung der kalibrierten, debayerten Lights
register pp_light

# Stacken mit Ausreißer-Ablehnung und additiver Skalierung
stack r_pp_light rej 3 3 -norm=addscale -output_norm -rgb_equal -out=result

# Ergebnis laden
load result

# Plattenlösung + echte Farbkalibrierung anhand Sternkatalog (Gaia)
# Braucht Internet. Falls FOCALLEN/XPIXSZ im FITS-Header fehlen, ggf.
# "platesolve -focal=<mm> -pixelsize=<µm>" statt "platesolve" nutzen.
platesolve
pcc

save result_stretched